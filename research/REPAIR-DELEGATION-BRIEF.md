# DOHNUT recovery — execution contract

User authorizes detailed delegated fixes across existing G:/Doh-Nut, preserving brand/UX, minimizing conductor tokens. No repeated clarification requests. This is implementation and verification, not another audit-only completion.

## Ground truth / boundaries
- Repo G:/Doh-Nut; Windows git-bash. Native git needs G:/Doh-Nut, not /g/Doh-Nut in git -C. Check live branch/status before edits. Existing dirty changes MUST be preserved; do not commit/push/reset/stash/delete user data. GOAL.txt is currently empty; do not assume it contains requirements.
- All earlier COMPLETED/PASS labels are untrusted. Hash uniqueness is not visual correctness. Four HTML concept mockups do NOT replace live PNGs. A security comment is not session auth. Changing Prisma provider/Decimal is not a tested migration. File existence/string checks are not behavioral tests. Pipeline exit 0 after tail is not reliable command success.
- Read relevant installed skills first, especially using-superpowers, systematic-debugging, test-driven-development, verification-before-completion and domain skills. Use official docs matching installed versions. Do not modify Hermes models/profiles/credentials or use paid services. No nested delegation. Keep outputs compact Malay with technical terms.
- Never read or print .env/auth tokens/customer data. Use existing runtime configuration without exposing values. No external production DB migrations, real charges, deployment, deletion, disabling products or invented product imagery without a clear approved source. Tests use isolated temporary databases and synthetic test fixtures explicitly labeled as fixtures, never presented as real transactions.
- Shared dirty workspace is deliberate: source writers are dependency-serialized. Discovery tasks read code only and write only their uniquely named research/recovery report. Never edit another task's ownership. Before a package install/generate/dev-server restart, inspect running processes and avoid unrelated processes. No blanket taskkill or dependency deletion.

## Required outcomes
1. Catalog single source of truth; every active SKU image decoded, visually inspected against name/description, traced from API/DB to browser; no duplicates/mismatch/tutorial placeholders asserted fixed without real replacement and browser verification. Inspect Matcha White Choco Sprinkle explicitly. Preserve original assets; asset gaps must be specific, not hidden.
2. Durable PostgreSQL architecture with compatible migrations, correct generated Prisma client, no production ephemeral /tmp fallback or runtime destructive schema bootstrap. Safe local test setup; don't use db:push currently containing --accept-data-loss.
3. Money exact through validation/calculation/storage/JSON/display/Billplz integer sen, not merely schema Decimal. Document rounding and overflow behavior, regression tests.
4. Order/payment inventory transaction integrity, idempotent order/bill/webhook handling including concurrent/replayed/invalid signatures/wrong amounts/provider failure, no oversell, no repeated paid effects.
5. Server-authenticated admin HttpOnly sessions, CSRF/origin validation, expiry/logout, no browser-stored master API key; customer ownership enforcement; AI input/body/rate/cost limits. No external auth service is inherently needed just to implement session security.
6. Persisted tracking transitions/events, authorized subscriptions and reads; no simulated timer state changes; reconnect uses DB; restrictive CORS. Stats paid-only with precise date/timezone/refund/cancellation semantics and behavioral tests.
7. Brand-preserving responsive accessible frontend, real reduced-motion logic (remove prior unused import/comment pseudo-fix only if confirmed), keyboard/focus/contrast/loading/error checks, actual rendered screenshot and console/network inspection.
8. Dependency audit covers source, scripts, mini-services, config, generated code and peer dependencies. Previous removal of date-fns/@dnd-kit/core etc may be unsafe. Test runner exists: package scripts currently use bun test, so missing vitest is not automatically a blocker. Fix tests to actual runner or justify minimal dependencies. Lint scoped to authored code, not blanket error suppression. CI runs Prisma generation, safe migration integration tests, lint, typecheck, behavioral tests, build.
9. Rewrite contradictory reports with current verified facts. Final acceptance includes browser homepage/catalog/cart, ownership-negative cases, local DB restart persistence, checkout sandbox/provider boundary and no production-ready claim until all gates truly pass.

## Evidence / handoff
Each task writes research/recovery/<lane>.md with observed defects, changed paths, commands + real exit codes, behavior verified, remaining blockers. Keep final handoff <=350 words; attach screenshot/report where meaningful. No repeated status appendices or invented PASS printouts. Failures include concise raw stderr. Run relevant tests; missing package/time is work to solve, not a completion reason. Code changes require review: structured kanban comment with paths/tests/diff evidence then block kind=needs_input reason='review-required: ...' per worker protocol. Do not mark unfinished code done. Independent reviewer/coordinator will handle review gates. Discovery-only reports may complete after evidence is verified. Genuine credential/approved-image gaps are blocked explicitly; still finish unrelated feasible work inside the task first.
